"""Handles logging configuration for the local-data-platform library.

This module sets up a simple logger that can be used across the library
to provide consistent and configurable logging output.
"""
import logging

log = logging.getLogger(__name__)